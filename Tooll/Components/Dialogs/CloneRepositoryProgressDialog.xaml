<Window x:Class="Framefield.Tooll.CloneRepositoryProgressDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Fetching operators from server" 
        Height="100" 
        Width="480"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner" >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>

        <Label Grid.Column="0" Grid.Row="0">No local operators found, so fetching them from server. This may take some seconds.</Label>
        <ProgressBar Grid.Column="0" Grid.Row="1" Name="XProgressBar"/>
        <TextBlock Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Name="XProgressText" />
    </Grid >

</Window>
